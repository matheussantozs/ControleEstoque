const express=require('express');const pool=require('../db/pool');const {auth}=require('../middleware/auth');const router=express.Router();router.use(auth);
router.get('/',async(req,res,next)=>{try{const [[totais]]=await pool.query(`SELECT COUNT(*) produtos,COALESCE(SUM(quantidade),0) unidades,COALESCE(SUM(quantidade*preco),0) valor FROM produtos`);const [[baixo]]=await pool.query('SELECT COUNT(*) n FROM produtos WHERE quantidade<=limite_minimo');const [[entradas]]=await pool.query("SELECT COALESCE(SUM(quantidade),0) n FROM movimentacoes WHERE tipo='ENTRADA' AND DATE(created_at)=CURDATE()");const [[saidas]]=await pool.query("SELECT COALESCE(SUM(quantidade),0) n FROM movimentacoes WHERE tipo='SAIDA' AND DATE(created_at)=CURDATE()");res.json({...totais,estoque_baixo:baixo.n,entradas_hoje:entradas.n,saidas_hoje:saidas.n})}catch(e){next(e)}});module.exports=router;
