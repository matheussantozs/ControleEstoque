const express=require('express');const pool=require('../db/pool');const {auth}=require('../middleware/auth');const router=express.Router();router.use(auth);
router.get('/estoque',async(req,res,next)=>{try{const [r]=await pool.query('SELECT codigo,nome,categoria,preco,quantidade,limite_minimo,(quantidade*preco) valor_estoque FROM produtos ORDER BY nome');res.json(r)}catch(e){next(e)}});
router.get('/movimentacoes',async(req,res,next)=>{try{const {inicio,fim}=req.query;if(!inicio||!fim||inicio>fim)return res.status(400).json({error:'Período inválido.'});const [r]=await pool.query(`SELECT DATE(m.created_at) data,m.tipo,SUM(m.quantidade) quantidade,COUNT(*) registros FROM movimentacoes m WHERE DATE(m.created_at) BETWEEN ? AND ? GROUP BY DATE(m.created_at),m.tipo ORDER BY data DESC`,[inicio,fim]);res.json(r)}catch(e){next(e)}});module.exports=router;
