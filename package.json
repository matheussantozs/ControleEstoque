{
    "name": "sistema-gerenciamento-estoque",
    "version": "1.0.0",
    "description": "Sistema de gerenciamento de estoque para mercadinhos de pequeno porte - IFAL",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "dev": "node --watch src/server.js",
        "seed": "node src/seed.js",
        "check": "node --check src/server.js && for f in src/routes/*.js src/middleware/*.js src/db/*.js src/*.js; do node --check \"$f\"; done",
        "setup": "node src/setup.js",
        "bootstrap": "npm run setup && npm run seed"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.21.2",
        "helmet": "^8.0.0",
        "jsonwebtoken": "^9.0.2",
        "mysql2": "^3.11.5"
    }
}
